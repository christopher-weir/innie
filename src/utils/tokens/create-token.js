'use strict';

module.exports = function( _chunk, _index ) {
    // console.log( _index );
    // console.log( _chunk );

    var token = {
        original    : _chunk,
        matches     : []
    };

    return token;
};
